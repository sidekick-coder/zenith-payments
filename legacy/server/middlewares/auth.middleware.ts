import { AuthMiddleware } from "@sidekick-coder/zenith-kit/server";

/** @deprecated use @sidekick-coder/zenith-kit/server's AuthMiddleware instead */
const authMiddleware = new AuthMiddleware()

export default authMiddleware;
